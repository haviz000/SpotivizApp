const config = {
    SPOTIFY_BASE_URL: 'https://api.spotify.com/v1',
    SPOTIFY_SCOPE: 'playlist-modify-private',
    RESPONSE_TYPE: "token",
    REDIRECT_URI: "https://spotiviz.vercel.app",
}

export default config;